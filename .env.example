# --- Environment Variables Example ---
# ไฟล์นี้เป็นตัวอย่างการตั้งค่า (ห้ามใส่รหัสผ่านจริงลงในไฟล์นี้)

# 1. การเชื่อมต่อฐานข้อมูล MongoDB (สำหรับเครื่อง Local)
# ถ้าใช้เครื่องตัวเอง ให้ใช้ค่านี้ได้เลย ไม่ต้องแก้
MONGODB_URI=mongodb://localhost:27017/ratchaprachanukroh41

# 2. การตั้งค่า Seed ข้อมูล (การสร้างข้อมูลตัวอย่างอัตโนมัติ)
# true = สร้างข้อมูลใหม่เมื่อเริ่ม Server, false = ไม่สร้าง
SEED_DB=false

# 3. ตัวอย่างสำหรับ MongoDB Atlas (Cloud)
# ถ้าจะใช้ Cloud ให้เอา # ออก แล้วเปลี่ยน <username>, <password> เป็นของจริงในไฟล์ .env
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/ratchaprachanukroh41?retryWrites=true&w=majority

# 4. ระบบ Admin และความปลอดภัย
# ข้อความลับสำหรับเข้ารหัส Session (ให้ตั้งยาวๆ มั่วๆ ในไฟล์จริง)
NUXT_SESSION_PASSWORD=change_this_to_a_complex_random_string_at_least_32_chars

# รหัสผ่านสำหรับ Admin (ให้ตั้งใหม่ในไฟล์จริง)
ADMIN_PASSWORD=change_me_to_your_secure_password
