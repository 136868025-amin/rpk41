# --- Environment Variables Example ---
# ไฟล์นี้เป็นตัวอย่างการตั้งค่า (ห้ามใส่รหัสผ่านจริงลงในไฟล์นี้)
# สร้างไฟล์ .env.local และใส่ค่าจริงในนั้น

# ==========================================
# Nuxt 3 Runtime Config (ใช้ NUXT_ prefix)
# ==========================================
# ค่าที่ขึ้นต้นด้วย NUXT_ จะ auto-map ไปยัง runtimeConfig
# เช่น NUXT_MONGODB_URI -> runtimeConfig.mongodbUri

# 1. MongoDB Connection
NUXT_MONGODB_URI=mongodb://localhost:27017/ratchaprachanukroh41

# 2. Cloudinary (Cloud Storage สำหรับรูปภาพและไฟล์)
# สมัครฟรีที่ https://cloudinary.com แล้วไปที่ Dashboard เพื่อรับค่าเหล่านี้
NUXT_CLOUDINARY_URL=cloudinary://API_KEY:API_SECRET@CLOUD_NAME
NUXT_CLOUDINARY_CLOUD_NAME=your_cloud_name
NUXT_CLOUDINARY_API_KEY=your_api_key
NUXT_CLOUDINARY_API_SECRET=your_api_secret

# 3. Public Config (ใช้ NUXT_PUBLIC_ prefix)
NUXT_PUBLIC_SEED_DB=false

# ==========================================
# ค่าอื่นๆ (ไม่ต้องใช้ NUXT_ prefix)
# ==========================================

# ระบบ Admin และความปลอดภัย
NUXT_SESSION_PASSWORD=change_this_to_a_complex_random_string_at_least_32_chars
ADMIN_PASSWORD=change_me_to_your_secure_password

# ==========================================
# ตัวอย่างสำหรับ MongoDB Atlas (Cloud)
# ==========================================
# NUXT_MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/ratchaprachanukroh41?retryWrites=true&w=majority

